// TODO: todos templates devem ser importados e exportado neste arquivo

// OBS: nao remova os comentario ele são utilizado para automatizar registro
//      dos template automaticamente.

const colunist = require('./colunistTemplate')
//newTemplateImport

module.exports = {
    colunist,
    //newTemplateRegister
}